# v1.3 · SkillOpt 集成设计 · 行为评测与技能文本优化

> 文档类型：设计文档（analysis + design），不含实现 commit。
> 发布日期：2026-08-10 · codename `skillopt-integration` · **additive，无 BREAKING CHANGE**
> 状态：📋 设计阶段（待批准 C2-C5 实施）

---

## 0. TL;DR

quick-knowledge 目前 4 个 CI job 全部是**静态结构校验**（frontmatter 字段、wikilink 死链、占位符残留、demo-vault 目录），**零行为测试**——CI 无法回答「用户说『记一下 cache 方案有问题』时，capture 是否真的写到 `00_inbox/ideas/` 并打 `type: idea`」。

本设计引入 **[microsoft/SkillOpt](https://github.com/microsoft/SkillOpt)**（文本空间技能优化器，MIT，PyPI `skillopt`）作为**行为评测 + 技能文本优化**引擎：

1. 写一个自定义 SkillOpt benchmark `quickkb`，把 14 个 SKILL.md 当作「被优化的权重」
2. 用 demo-vault 作为 ground truth，rollout = 跑技能 → 检查产物（路径 / frontmatter / citation）
3. SkillOpt 的 `Rollout → Reflect → Aggregate → Select → Update → Gate` 流水线自动产出 `best_skill.md`
4. 永不自动部署——产出物供人工 review 后单独 commit

**MVP（v1.3.0）**：仅 `quick-kb-capture` × 20 golden cases × mock 后端，验证「能跑通」。
**v1.3.1**：扩展到 query + ingest。
**v1.4**：接入 SkillOpt-Sleep 真实会话闭环（先过隐私 ADR）。

---

## 1. 目标

### 1.1 解决的问题

| 问题 | 现状 | SkillOpt 引入后 |
|------|------|----------------|
| SKILL.md 改了一行，capture 行为是否退化？ | ❌ 无法察觉 | ✅ held-out gate 给出 hard/soft 分对比 |
| 14 个技能的自然语言是否「写得到位」？ | ❌ 主观判断 | ✅ reflect 阶段产出可读的 SKILL.md 编辑提案 |
| 新加入的 v1.2 步骤 2.5 / 3.5 是否真的在正确场景触发？ | ❌ 无测试 | ✅ golden case 覆盖触发条件 |
| 用户的真实 capture 输入模式能否反哺 SKILL.md？ | ❌ 完全没有 | ✅ SkillOpt-Sleep 收割会话挖掘（v1.4） |

### 1.2 不解决的问题（out of scope）

- ❌ 不替换任何现有 CI 检查（结构校验保留）
- ❌ 不在 PR 上阻塞 merge（行为评测是参考，不是 gate）
- ❌ 不自动 commit best_skill.md（永远人工 review）
- ❌ 不做个性化训练（每个用户自己 vault 训自己 skill，留 v2.0+）
- ❌ 不修改 SkillOpt 引擎源码（保持 pip install 上游）

---

## 2. SkillOpt 背景

### 2.1 是什么

**SkillOpt** 是 Microsoft 开源的「文本空间技能优化器」。核心类比：

| 神经网络 | SkillOpt |
|---------|---------|
| 权重 (weights) | skill markdown 文档 |
| 前向传播 (forward) | rollout（跑技能） |
| 梯度 (gradient) | reflect（反思轨迹） |
| 梯度剪裁 (clipping) | select（编辑选择） |
| 验证集 (validation) | held-out gate |
| 多轮 epoch | 多轮 update |

输出物：`best_skill.md`（300-2000 tokens），部署到**冻结的目标模型**上。

### 2.2 双引擎

| 引擎 | 命令 | 在 quick-knowledge 的用途 |
|------|------|--------------------------|
| **SkillOpt 研究引擎** | `skillopt-train` / `skillopt-eval` | 在自定义 `quickkb` benchmark 上做有监督的 SKILL.md 优化 |
| **SkillOpt-Sleep** | `skillopt-sleep` | 夜间回顾本地 Claude Code 真实会话，挖掘反复出现的 capture/daily pattern，长出 SKILL.md 的「经验补丁」（**v1.4 接入**） |

两个引擎独立可选用。

### 2.3 6 阶段流水线

```
Rollout → Reflect → Aggregate → Select → Update → Gate
```

| 阶段 | 在 quick-knowledge 语境下 |
|------|-------------------------|
| Rollout | 用当前 SKILL.md 作 system context，对一批 golden case 跑 quick-kb-{skill}（例如「记一下 X」→ 看 capture 写出什么文件） |
| Reflect | 优化器读 `conversation.json` 轨迹，找出哪里出错（source 类型识别错 / frontmatter 缺字段 / 路径走错） |
| Aggregate | 多条反思合并成对 SKILL.md 的编辑提案 |
| Select | 在多个候选编辑中按得分剪裁 |
| Update | 把选中的编辑合并进 SKILL.md |
| Gate | 在 held-out 验证集上跑一次，不过就回滚（防过拟合） |

### 2.4 自定义 benchmark 契约

SkillOpt 内置 6 个 benchmark（DocVQA/ALFWorld/OfficeQA/SearchQA/SpreadsheetBench/LiveMathematicianBench）**与 PKB 场景无关**——都是「外部任务 QA」范式，无法验证「文件真的被写到对的地方」。

但 SkillOpt 暴露了自定义 benchmark 接口（`docs/guide/new-benchmark.md`），需实现 **4 件套**：

1. **`SplitDataLoader` 子类** — 加载 train/val/test 用例
2. **rollout helper** — 跑目标模型，把每次对话轨迹持久化到 `<rollout_dir>/predictions/<id>/conversation.json`
3. **`EnvAdapter` 子类** — 接线 dataloader + rollout 到 SkillOpt 生命周期
4. **YAML config** — 引用 env + 标准 train/optimizer/gradient 旋钮

**关键约束**：
- 打分逻辑写在 rollout helper（没有 `evaluate()` ABC）；`hard`（0/1 或 [0,1]）+ `soft`（[0,1]）是优化器唯一信号
- 必须持久化 conversation.json，否则 reflect 无法生成补丁
- 参考样板：`skillopt/envs/officeqa/`（最简完整实现，~200 行）

### 2.5 后端选择

| 后端类型 | 选项 | 适用性 |
|---------|------|-------|
| chat | `openai_chat` / `claude_chat` / `qwen_chat` / `minimax_chat` / `openai_compatible` | 把 SKILL.md 当 system prompt 单轮对话。简单但失真（技能实际跑在 Claude Code 里有文件读写） |
| exec | `claude_code_exec` / `codex_exec` | 真实复现：起 Claude Code 子进程，让技能真的写文件到 vault 副本。**MVP 首选** |

### 2.6 SkillOpt-Sleep 安全边界（v1.4 提前标注）

- **本地只读收割**：harvest 本地 `~/.claude/projects/` / `~/.codex/` / `~/.cursor/` 等会话 JSONL
- **mock 后端零网络**：`skillopt-sleep dry-run --backend mock` 不发任何 provider 请求
- **真实后端会把截断的会话发给 provider**——必须先 `harvest` → 人工 review/redact → 标 `"reviewed": true` → 才能用 real backend replay
- **v1.3 完全不接入 Sleep**；v1.4 启动前必须先过 ADR-018 隐私设计

---

## 3. 范围

### 3.1 in scope（v1.3.0 MVP）

- 新增顶层目录 `bench/`（与 `scripts/` 平级）
- 实现自定义 benchmark `quickkb`（dataloader + rollout + adapter + scoring）
- **仅接入 `quick-kb-capture`** 一个技能
- **20 个 golden cases**（覆盖 idea/meeting/ai-dialog/reading/webclip 5 种 source type × 4 用例）
- 评分器：仅 `routing` + `frontmatter`（citation 留 v1.3.1）
- 后端：mock（CI/本地）+ claude_code_exec（仅本地手跑）
- 配置：`bench/configs/capture-default.yaml`
- CI workflow：`.github/workflows/skillopt.yml`（nightly mock，不阻塞 PR）
- 5 语种 README 加「Behavior testing powered by SkillOpt」段
- 版本 1.2.0 → 1.3.0

### 3.2 推后（v1.3.1）

- 接入 `quick-kb-query`（citation scorer）+ 20 case
- 接入 `quick-kb-ingest` + 15 case
- nightly 加真实 backend 对照（mock 仍跑 PR）

### 3.3 推后（v1.4）

- SkillOpt-Sleep 收割本地会话
- 隐私 ADR + `--redact` 默认开
- 真实数据闭环

### 3.4 永不做（out of scope）

- ❌ 14 个技能全覆盖（先验证 1 个能跑通）
- ❌ 自动 commit best_skill.md 到主分支
- ❌ 在 PR 上阻塞 merge
- ❌ 个性化 skill 训练（v2.0+ 候选）
- ❌ 改 SkillOpt 引擎源码

---

## 4. 设计要点

### 4.1 顶层组件图

```
┌─────────────────────────────────────────────────────────────────┐
│                  quick-knowledge repo                           │
│                                                                 │
│  skills/quick-kb-{14}/SKILL.md  ←  被优化的「权重」            │
│         ▲                                                       │
│         │ 读 / 写（best_skill.md 经人工 review 后替换）        │
│         │                                                       │
│  ┌──────┴──────────────────────────────────────────────┐        │
│  │  bench/  （新增顶层目录）                            │        │
│  │                                                      │        │
│  │  quickkb/                   ← SkillOpt 自定义 env   │        │
│  │    ├── dataloader.py        ← 加载 cases/           │        │
│  │    ├── rollout.py           ← 跑技能 + 调度评分     │        │
│  │    ├── adapter.py           ← EnvAdapter 接线       │        │
│  │    └── scoring/             ← rubric 评分器         │        │
│  │         ├── routing.py                              │        │
│  │         ├── frontmatter.py                          │        │
│  │         └── citation.py     （v1.3.1）              │        │
│  │                                                      │        │
│  │  cases/capture/*.json × 20  ← golden test cases     │        │
│  │  cases/_schema.json         ← JSON schema           │        │
│  │  configs/capture-default.yaml ← MVP 训练配置        │        │
│  │  harness/clone-vault.mjs    ← 复制 demo-vault 副本  │        │
│  │  reports/<run-id>/          ← 每次运行的产物        │        │
│  └──────────────────────────────────────────────────────┘        │
│                                                                 │
│  .github/workflows/                                             │
│    ├── ci.yml                   ← 不动（结构校验）              │
│    └── skillopt.yml             ← 新增（行为评测）              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ pip install skillopt
                            ▼
              ┌──────────────────────────────┐
              │  SkillOpt 引擎（外部依赖）   │
              │  - skillopt-train            │
              │  - skillopt-eval             │
              │  - skillopt-sleep（v1.4+）   │
              └──────────────────────────────┘
```

### 4.2 单次 rollout 数据流

```
golden case (input: "记一下 cache 方案有问题")
   │
   ▼
QuickkbDataLoader.load_split_items() → item dict
   │
   ▼
rollout.run_batch(items, skill_content=current_SKILL_md)
   │
   ├── clone-vault.mjs → 临时 demo-vault 副本（不动 ground truth）
   │
   ├── claude_code_exec 后端
   │     system = current_SKILL_md
   │     user   = item.input
   │     cwd    = 临时 vault 副本
   │
   ├── 技能执行：写文件到副本 vault
   │
   ├── scoring/ 评分：
   │     frontmatter.py → 检查写入文件 frontmatter 字段
   │     routing.py      → 检查写入路径匹配 expected_path_glob
   │
   ├── 写 conversation.json（reflect 必需）
   │
   └── 返回 {id, hard, soft, predicted_answer, ...}
```

### 4.3 评分器设计

每个评分器返回 `(hard: float, soft: float)`：
- **hard** 取最严 fail-fast（任一 0 即 0）——是否完全正确
- **soft** 取加权平均——部分正确程度（让优化器即使 hard=0 也有梯度方向）

**`routing.py`** — 路径评分

```python
def score(actual_path: str, expected_path_glob: str) -> tuple[float, float]:
    """例：expected '00_inbox/ideas/*.md'"""
    match = fnmatch(actual_path, expected_path_glob)
    return (1.0, 1.0) if match else (0.0, 0.0)
```

**`frontmatter.py`** — 字段评分

```python
def score(actual_fm: dict, required_fields: dict) -> tuple[float, float]:
    """required_fields 例：{type: '^idea$', title: '.*cache.*'}"""
    score_per_field = []
    for k, expected_regex in required_fields.items():
        actual = actual_fm.get(k)
        ok = bool(actual and re.match(expected_regex, str(actual)))
        score_per_field.append(1.0 if ok else 0.0)
    hard = 1.0 if all(score_per_field) else 0.0
    soft = sum(score_per_field) / len(score_per_field)
    return hard, soft
```

**`citation.py`**（v1.3.1） — query 命中度

```python
def score(answer: str, ground_truth_ids: list[str]) -> tuple[float, float]:
    cited = extract_wikilinks(answer)
    hit = len(set(cited) & set(ground_truth_ids))
    return (1.0, hit / len(ground_truth_ids)) if hit else (0.0, 0.0)
```

### 4.4 Golden case JSON schema

```json
{
  "id": "capture-001-typed-idea",
  "task_type": "capture",
  "source_type": "idea",
  "input": "记一下 cache 方案有问题",
  "expected": {
    "path_glob": "00_inbox/ideas/*.md",
    "frontmatter": {
      "type": "^idea$",
      "title": ".*cache.*"
    },
    "content_contains": ["cache"],
    "should_trigger_polish": true
  },
  "notes": "v1.2 验证：字符数<50，应触发润色提议（步骤 2.5）"
}
```

### 4.5 YAML 配置

```yaml
# bench/configs/capture-default.yaml
env: quickkb_capture

data:
  data_root: bench/cases/capture/
  split_mode: ratio
  split_ratio: [0.6, 0.2, 0.2]   # train / val / test

model:
  backend: claude_code_exec
  optimizer: claude-sonnet-4-6
  target: claude-sonnet-4-6

train:
  epochs: 3
  batchsize: 8
  learning_rate: 0.3
  max_edit_tokens: 500

gate:
  enabled: true
  metric: hard
  min_improvement: 0.05          # 相比 baseline 至少提升 5%

output:
  out_dir: bench/reports/
  deploy_skill: false            # MVP 不自动部署
```

### 4.6 GitHub workflow

`.github/workflows/skillopt.yml`：
- 触发：`workflow_dispatch` + `schedule(cron: 0 2 * * *)`（nightly 02:00 UTC）
- 仅跑 mock 后端
- 失败不标红 X（`continue-on-error: true`）
- 产物 `bench/reports/<run-id>/` 上传 artifacts，保留 30 天

---

## 5. 实施清单

### 5.1 C1 · 设计文档（本次交付）

| 文件 | 改动 |
|------|------|
| `docs/dev/v1.3-skillopt-integration.md` | 新增（本文件） |
| `docs/dev/README.md` | §1 阶段表 + §7 文档清单加 v1.3 行 |

**Commit**：`docs(v1.3): C1 SkillOpt 集成设计文档`

### 5.2 C2 · benchmark 主体

| 文件 | 内容 |
|------|------|
| `bench/README.md` | 如何跑（mock + real 两种） |
| `bench/quickkb/__init__.py` | 包标识 |
| `bench/quickkb/dataloader.py` | QuickkbDataLoader（SplitDataLoader 子类，~40 行） |
| `bench/quickkb/rollout.py` | run_batch + _rollout_one + 评分调度（~120 行） |
| `bench/quickkb/adapter.py` | QuickkbEnvAdapter（EnvAdapter 子类，~60 行） |
| `bench/quickkb/scoring/__init__.py` | 评分器出口 |
| `bench/quickkb/scoring/routing.py` | path glob 评分（~25 行） |
| `bench/quickkb/scoring/frontmatter.py` | frontmatter 字段评分（~40 行） |
| `bench/harness/clone-vault.mjs` | 复制 demo-vault 到 tmpdir（~30 行） |

**Commit**：`feat(bench): C2 quickkb benchmark 主体`

### 5.3 C3 · golden cases

| 文件 | 内容 |
|------|------|
| `bench/cases/capture/*.json` × 20 | golden test cases |
| `bench/cases/_schema.json` | case JSON schema |
| `bench/configs/capture-default.yaml` | MVP 训练配置 |
| `bench/configs/_base/skill-template.md` | 基准 SKILL.md（mock rollout 用） |

**Commit**：`test(bench): C3 capture 20 golden cases + MVP config`

### 5.4 C4 · CI workflow

| 文件 | 内容 |
|------|------|
| `.github/workflows/skillopt.yml` | nightly mock eval |
| `bench/reports/.gitkeep` | 报告目录占位 |
| `.gitignore` | 加 `bench/reports/*` + `bench/.tmp-vault/` |
| `requirements-bench.txt` | `skillopt>=0.2.0` |

**Commit**：`ci(bench): C4 SkillOpt nightly workflow + gitignore`

### 5.5 C5 · 版本同步

| 文件 | 改动 |
|------|------|
| `.claude-plugin/plugin.json` + `marketplace.json` | version 1.2.0 → 1.3.0；phases 加 v1.3 |
| `README.md` + 4 个外语 README | 加「Behavior testing powered by SkillOpt」段 |
| `docs/DESIGN.md` | §6.11 新增 + ADR-017 + §13 路线图加 v1.3 |
| `docs/CHANGELOG.md` | 顶部 v1.3.0 节 |

**Commit**：`docs(v1.3): C5 plugin version bump + 5 语种 README + DESIGN/CHANGELOG`

---

## 6. 关键设计决策与权衡

### 6.1 为什么写自定义 benchmark 而非用内置？

SkillOpt 内置 benchmark 全是「外部 QA」范式，无法验证「文件真的被写到对的地方」。`officeqa` 最接近但仍是 QA，无法表达「检查 capture 是否把 idea 写到了 `00_inbox/ideas/`」这类路径断言。

### 6.2 为什么 MVP 用 mock 后端？

- **成本**：3 epoch × 8 batch × 20 case = 480 次 LLM 调用，真实跑约 $5-20，MVP 不值得
- **稳定性**：真实后端有 rate limit / 网络抖动 / Claude Code CLI 版本漂移
- **目的错位**：MVP 是验证「能跑通」不是「能优化」，mock 够用

### 6.3 为什么 best_skill.md 永不自动部署？

SkillOpt gate 是「相比 baseline 提升 5%」，但**不能保证语义不漂移**——优化器可能为了刷分让 capture 变保守（写更少字段以提高 frontmatter 完整率），与产品意图相反。永远人工 review diff 后单独 commit。

### 6.4 为什么不一开始就上 SkillOpt-Sleep？

Sleep 涉及**收割本地会话**，存在隐私敏感面（即使 mock 后端，harvest 也读 `~/.claude/projects/`）。必须先：
- 写清楚数据流 ADR（哪个进程读哪些文件、传给谁）
- 默认 `--redact` 开启
- 用户显式 opt-in `--backend real`

→ v1.4 的活，不在 v1.3 MVP。

### 6.5 与现有 v0.1-v1.2 测试的关系

| 现有测试 | SkillOpt 评测 | 关系 |
|---------|-------------|------|
| `check-frontmatter.mjs` | `scoring/frontmatter.py` | **互补**：CI 抓静态违规，SkillOpt 抓动态生成质量 |
| `check-links.mjs` | （v1.3.1 加 `scoring/link.py`） | 同上 |
| demo-vault placeholder check | gate 用 demo-vault 作 ground truth | 复用 demo-vault |

**SkillOpt 评测不替代任何现有测试，只做加法。**

---

## 7. 验证

### 7.1 C2 完成后（benchmark 主体可跑）

```bash
pip install -r requirements-bench.txt
python -m bench.quickkb.dry-run --config bench/configs/capture-default.yaml
# 期望：20 case 全跑完，无 Exception，输出 bench/reports/<id>/report.md
```

### 7.2 C3 完成后（golden cases 齐全）

```bash
node bench/harness/validate-cases.mjs bench/cases/capture/
# 期望：20 case 全部符合 _schema.json，0 error

node bench/harness/coverage.mjs bench/cases/capture/
# 期望：idea/meeting/ai-dialog/reading/webclip 各 ≥ 3 case
```

### 7.3 C5 完成后（发布前）

```bash
# CI 4 个原 job 全绿
gh workflow run ci.yml

# SkillOpt nightly workflow 能起来（mock）
gh workflow run skillopt.yml
gh run watch
# 期望：completed, conclusion=success

# 版本号同步
grep -rn "1\.3\.0\|v1\.3" .claude-plugin/ docs/ README*.md bench/README.md | wc -l
# 期望：≥ 6
```

### 7.4 发布关卡

```bash
git tag v1.3.0
git push origin v1.3.0
gh release create v1.3.0 --notes-file bench/reports/<final-run-id>/report.md
```

---

## 8. 单元测试场景（capture MVP）

| Case ID | 用户输入 | 期望 |
|---------|---------|------|
| `001-typed-idea` | 「记一下 cache 方案有问题」 | 写 `00_inbox/ideas/*.md`，frontmatter `type: idea`，触发 v1.2 润色提议（字符 < 50） |
| `002-typed-meeting` | 「开会决策选 PostgreSQL」 | 写 `00_inbox/meetings/*.md`，`type: meeting`，触发润色 |
| `003-ai-dialog` | 「和 AI 聊了 RAG 选型」 | 写 `00_inbox/ai-dialogs/*.md`，`type: ai-dialog` |
| `004-reading` | 「读了 ddia 第 4 章」 | 写 `00_inbox/reading-notes/*.md`，`type: reading-note` |
| `005-webclip` | 「抓 https://example.com/article」 | 写 `00_inbox/clips/*.md`，**不触发润色**（外部来源逐字保留） |
| `006-long-input` | 「[50+ 字详细描述...]」 | **不触发润色**（字符 ≥ 50），走原流程 |
| `007-explicit-polish` | 「润色记一下 X」 | 强制触发（用户显式关键词） |
| `008-duplicate` | 与 demo-vault 已有 idea 高度相似 | 触发去重检测（步骤 3） |
| ... | ... | ... |

完整 20 case 见 `bench/cases/capture/`（C3 阶段产出）。

---

## 9. 风险与缓解

| 风险 | 等级 | 缓解 |
|------|-----|------|
| **R1** SkillOpt 上游 API 漂移（v0.2 → v0.3 改 EnvAdapter 接口） | 中 | 锁版本 `skillopt==0.2.*`；adapter 层加抽象 |
| **R2** Claude Code CLI 版本与 `claude_code_exec` 后端不匹配 | 中 | CI 用 `claude-code@latest`，本地用项目 pinned 版本；rollout 加 sanity check |
| **R3** mock 后端测出的优化在真实环境失效 | 高 | MVP 明确「不部署」；v1.3.1 加 nightly 真实 backend 对照 |
| **R4** Golden case 偏，过拟合到 case | 中 | val/test held-out；case 写作遵循「真实用户会怎么说」原则 |
| **R5** 评分器过严（hard 全 0）→ 训练不收敛 | 中 | soft 分始终有梯度；MVP 先 eval 不 train |
| **R6** SkillOpt-Sleep 隐私泄露（v1.4 提前标注） | 高 | v1.3 完全不接入；v1.4 启动前必须过 ADR-018 |
| **R7** 部署 best_skill.md 后行为退化 | 高 | 永远人工 review；commit message 必须引用 report.md |

---

## 10. 不可改白名单

1. 不修改 `skills/*/SKILL.md` 现有版本（除非 best_skill.md 经人工 review 后单独 commit）
2. 不修改 `.github/workflows/ci.yml`（4 个结构检查 job 保持原样）
3. 不修改 `examples/demo-vault/`（仅 read-only ground truth；rollout 写临时副本）
4. v1.3 不接入 SkillOpt-Sleep（隐私边界未定）
5. 不自动部署 best_skill.md（永远人工 review）

---

## 11. 后续阶段

| 阶段 | 内容 | 依赖 |
|------|------|------|
| **v1.3.0**（本设计） | capture × 20 case + mock 后端 + nightly workflow | 本文档批准 |
| **v1.3.1** | query + ingest 接入；nightly 加真实 backend 对照 | v1.3.0 跑通 |
| **v1.4** | SkillOpt-Sleep 本地会话收割；ADR-018 隐私设计 | v1.3.x 稳定运行 ≥ 1 个月 |
| **v2.0+** | 个性化训练（用户 vault 训自己 skill） | 社区反馈验证 |

---

## 12. 参考资料

- **SkillOpt 仓库**：https://github.com/microsoft/SkillOpt
- **SkillOpt 文档**：`docs/guide/new-benchmark.md`（4 件套实现指南）、`docs/guide/new-backend.md`（后端契约）、`docs/sleep/README.md`（Sleep 工作流）
- **SkillOpt 最简样板**：`skillopt/envs/officeqa/`（完整 benchmark 实现，~200 行）
- **SkillOpt ABC**：`skillopt/datasets/base.py`（SplitDataLoader）、`skillopt/envs/base.py`（EnvAdapter）
- **PyPI**：`pip install skillopt`（v0.2.0）
- **quick-knowledge 现有 CI**：`.github/workflows/ci.yml` + `scripts/check-{frontmatter,links}.mjs`
- **quick-knowledge demo-vault**：`examples/demo-vault/`（gate 的 ground truth）
